docker logs CONTAINER
        ↓
View container output

docker logs -f CONTAINER
        ↓
Follow new output

docker logs --tail 100 CONTAINER
        ↓
Show last 100 lines

docker logs -t CONTAINER
        ↓
Show timestamps

docker compose logs
        ↓
Logs from Compose services

docker compose logs app
        ↓
Logs from one Compose servicedocker logs CONTAINER
        ↓
View container output

docker logs -f CONTAINER
        ↓
Follow new output

docker logs --tail 100 CONTAINER
        ↓
Show last 100 lines

docker logs -t CONTAINER
        ↓
Show timestamps

docker compose logs
        ↓
Logs from Compose services

docker compose logs app
        ↓
Logs from one Compose service